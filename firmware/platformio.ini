; PlatformIO configuration placeholder
; Fill this after the firmware target and upload method are finalized.

[platformio]
default_envs = atmega328p

[env:atmega328p]
platform = atmelavr
board = ATmega328P
framework = arduino
board_build.f_cpu = 16000000L

; Upload through USBasp or another AVR ISP programmer.
; upload_protocol = usbasp
